# ATM System (C++)

A console-based **ATM System** written in **C++** using **file handling**, allowing users to log in, withdraw, deposit, and check balance.  
The system loads clients from a text file and updates their data after each transaction.

## Features
-  Login system (Account Number + PIN)
-  Quick Withdraw (20 → 1000)
-  Normal Withdraw (custom amount, multiple of 5)
-  Deposit money
-  Check balance
-  Persistent storage using `Clients.txt`
-  Clean modular structure using functions + `sClient` struct

